services:
  simplesocks5:
    build:
      context: .
      dockerfile: Dockerfile.debian  # build using Debian
#      dockerfile: Dockerfile.alpine # build using alpine
    ports:
      - "1080:1080"
    volumes:
      - ./3proxy.cfg:/etc/3proxy/3proxy.cfg
    restart: unless-stopped
